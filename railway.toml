[build]
builder = "nixpacks"

[build.nixpacks]
phpVersion = "8.3"
extensions = ["gd", "zip", "mbstring", "pdo_mysql"]

[deploy]
startCommand = "php artisan migrate:fresh --seed --force && php artisan serve --host=0.0.0.0 --port=${PORT}"
