[build]
builder = "nixpacks"

[build.nixpacks]
phpVersion = "8.3"

[build.nixpacks.extensions]
gd = true
zip = true
mbstring = true

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=${PORT}"
